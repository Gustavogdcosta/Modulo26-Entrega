function Multiplica (a:number, b:number): number {
    return a*b;
}

function nome(Nomedapessoa:string):string {
    return Nomedapessoa;
}

console.log (Multiplica (10,2));
console.log (`olá ` + nome('Gustavo'));